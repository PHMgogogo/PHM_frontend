# PHM 开放式架构平台 - Vue 前端需求说明

## 一、项目背景

开发一个基于 Vue 的 PHM（故障预测与健康管理）开放式架构平台前端页面。

平台需要兼容多种飞机构型，因此系统中的“飞行器”和“构型”是核心实体。

---

# 二、页面整体布局

首页（Homepage）由两个区域组成：

| 区域    | 宽度占比 | 说明    |
| ----- | ---- | ----- |
| 左侧导航栏 | 15%  | 深蓝色背景 |
| 右侧内容区 | 85%  | 白色背景  |

整体风格：

* 主色调：蓝色系
* 背景色：白色
* 左侧导航栏：深蓝色
* 选中状态 / 按钮 / 交互元素：蓝色高亮
* 风格简洁、现代、偏工业化平台风格

---

# 三、左侧导航栏

导航栏包含 3 个菜单项。

每个菜单项由：

* emoji 或图标
* 文本标题

组成。

菜单如下：

1. 飞行器管理（默认选中）
2. 知识库管理
3. 实时监控

建议数据结构：

```ts
const menus = [
  {
    key: 'aircraft',
    icon: '✈️',
    title: '飞行器管理'
  },
  {
    key: 'knowledge',
    icon: '📚',
    title: '知识库管理'
  },
  {
    key: 'monitor',
    icon: '📡',
    title: '实时监控'
  }
]
```

要求：

* 支持菜单切换高亮
* 默认进入“飞行器管理”
* 当前仅需要完整实现“飞行器管理”页面
* 其他页面可以先使用占位内容

---

# 四、飞行器管理页面

## 1. 页面结构

飞行器管理页面分为：

| 区域    | 高度占比 |
| ----- | ---- |
| 顶部功能区 | 10%  |
| 下方内容区 | 90%  |

---

## 2. 顶部功能区

顶部功能区左右布局：

### 左侧：搜索框

功能：

* 用户输入关键字
* 实时筛选飞行器列表

搜索范围：

* 飞行器名称
* 飞行器属性
* 飞行器构型名称

---

### 右侧：添加飞行器按钮

按钮文字：

```txt
添加飞行器
```

点击后：

* 打开弹窗表单（Dialog / Modal）

---

# 五、飞行器列表展示

内容区使用：

* Card 卡片布局
* 支持响应式排列
* 每张卡片代表一台飞行器

每张卡片展示：

* 飞行器名称
* 飞行器属性
* 所属构型（可选）
* 创建时间（可选）
* 简略状态信息（可选）

建议卡片风格：

* 白色背景
* 阴影
* hover 动效
* 圆角

---

# 六、添加飞行器弹窗

点击“添加飞行器”后，弹出表单。

---

## 表单字段

### 1. 飞行器名称（必填）

类型：

```txt
input
```

要求：

* 非空校验

---

### 2. 飞行器属性（必填）

类型：

```txt
select 或 input
```

暂定属性示例：

```txt
固定翼
旋翼机
无人机
运输机
战斗机
侦察机
```

支持后续扩展。

---

### 3. 所属构型（可选）

类型：

```txt
select
```

来源：

* 用户已创建的构型列表

功能：

* 可以直接选择已有构型
* 可以创建新构型

---

# 七、构型管理功能

## 设计要求

由于当前已经处于“添加飞行器”的弹窗中：

不要再次简单弹出新的独立窗口。

推荐方案：

* 在当前表单内部展开一个“构型管理区域”
* 或使用 Drawer / Collapse / 内嵌子面板

避免多层 Modal 造成交互混乱。

---

# 八、构型（Configuration）实体定义

“构型”用于定义：

用户上传飞机数据（.CSV）时：

* 哪一列是“飞行时间”
* 哪一列对应哪个部件的时序数据

本质上：

构型 = 一组键值对映射关系。

---

## 构型数据结构示例

```ts
{
  configName: "某型无人机构型",

  mappings: {
    "飞行时间": 1,

    "发动机温度": 23,

    "机翼振动": 25,

    "油压": 67
  }
}
```

---

# 九、构型管理功能需求

在构型管理区域中，支持：

## 1. 添加构型

用户需要填写：

### 构型名称

必填。

---

### 字段映射（核心功能）

用户动态添加键值对：

| 键（业务字段） | 值（数据列号）     |
| ------- | ----------- |
| 飞行时间    | 1 |

特殊规则：

### “飞行时间”字段必须存在

即：

```txt
key = 飞行时间
```

必须有对应的数据列。

例如：

```txt
飞行时间 -> 1
```

这是必填映射。

---

其他字段可选，例如：

```txt
发动机温度 -> 23
机翼振动 -> 25
```

支持：

* 动态新增字段
* 动态删除字段

---

## 2. 删除构型

支持删除已有构型。

删除前需要：

```txt
确认提示
```

---

# 十、创建飞行器流程

用户流程如下：

1. 点击“添加飞行器”
2. 填写飞行器基础信息
3. 选择已有构型 或 创建新构型
4. 完成构型配置
5. 点击“创建”
6. 新飞行器出现在卡片列表中

---

# 十一、建议的数据结构

## 飞行器

```ts
interface Aircraft {
  id: string

  name: string

  type: string

  configId?: string

  configName?: string

  createdAt?: string
}
```

---

## 构型

```ts
interface AircraftConfig {
  id: string

  configName: string

  mappings: Record<string, string>
}
```

---

# 十二、额外要求

* 页面需要有良好的层次感
* 注意工业平台风格
* 注意表单校验
* 注意组件状态管理
* 代码结构清晰
* 适当拆分组件
* 保持后续易扩展性
* 初期数据可使用 mock 数据
* 不需要后端接口，可先本地状态管理实现
