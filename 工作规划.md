你需要重构飞行器工作区内容（即AircraftWorkspace），由原先的选项“对话入口、数据管理”变更为“任务管理，数据入口”（原有的对话入口逻辑先隐藏，不要删除！）。在本次会话中**我们仅关心前端UI设计**。数据管理的业务暂时不变，“任务管理”模块需要展示一个任务列表，顶部功能区（占15vh左右）支持“按名称搜索任务”（一个占宽度50%左右search组件就好），“创建任务”。点击“创建任务”，应当在一个Loading动效（后端此时会进行耗时操作，我们不关心，此处先写死wait2秒）后弹出一个form dialog（可以复用AddAircraftDialog的样式），需要提供的信息有“姓名”（必填）“描述”（选填）。点击确认后创建一个任务。在内容区中展示任务列表，每一个任务元素（应该占10vh左右）左侧应该展示黑色大号字体（位于上方）的任务名（20字符截断）和灰色小号字体（位于下方）的任务描述（自定截断字符数）。右侧应该存在“编辑”“配置”“删除”三个功能按钮，编辑按钮应该有较强视觉引导，比如编辑蓝色填充/配置蓝框白填充/删除红色填充

受限于上下文容量，我即将新开启一个会话设计前端与后端的对接步骤。请将本会话中关键信息提炼为一个context.md文档提供给下一轮，内容应尽可能精练。

在上一轮对话中设计了飞行器工作区的UI重构，本轮中需要设计前后端对接，你需要先查看 @/d:/AALynx'sWorkspace/PHM_new/context.md 获取上下文信息。进入“任务管理”页面后应该调用一次/api/tasks获取所有任务。因为任务管理后端地址位于本地127.0.0.1:8000上，因此你需要使用client工厂新建一个baseurl为/task的client，然后在vite代理时将路径重写为/api。在创建任务用例中，点击“创建任务”按钮后，后端流程应该为调用http://192.168.31.13:8001远程服务器的Get /api/highlevel接口，返回json中的HTTP状态码注明这个算法实例是否成功启动，若成功，需要提取返回值中的内容。提取“instance_id”字段并短暂保存，提取“file_path”字段并替换前缀中的“/mnt/d”为“\\192.168.31.13”，目录分隔符使用了正斜杠/，需要处理为windows理解的反斜杠。随后你需要用处理后的路径作为工作目录连接opencode，并在飞行器工作区存在时保持连接的持久性（这一点需要在palnmode下评估），保证不因messagefeed页面的销毁而销毁。随后你需要获取opencode返回的session_id。最后你需要向本地的任务管理后端发送POST /api/tasks请求，传入前端表单中获取的{"name": "string","description": "",以及在之前任务中保存的"session_id": "","instance_id": ""}，在等待后端返回200Response后视为创建成功。以下是可供参考的相关接口文档 @/d:/AALynx'sWorkspace/PHM_new/openapi_task.json @/d:/AALynx'sWorkspace/PHM_new/openapi_instance.json 和部分关键接口返回信息 @/d:/AALynx'sWorkspace/PHM_new/response_1781153885601.json 。

在上一轮对话中设计了飞行器工作区的对话入口，本轮中需要修改一些设计模式，你需要先查看 @/d:/AALynx'sWorkspace/PHM_new/context.md 获取上下文信息。当前的cookie持久化存储了currentTask和workDir，我在分析后发现存在一些冗余设计。调整结果为：work_dir作为字段存储到task实体当中，可以通过GET /api/tasks获取。那么现在cookie只需要存储currentTask（对应值为task_id）。更新后的接口文档查询 @/d:/AALynx'sWorkspace/PHM_new/openapi_task.json 。请根据新的设计模式进行更新。

目前存在跳转会话的高耗时问题
还有，<DataPanel/>在存在opencode会话连接时的渲染一定是被Web接口（或者其他耗时长）阻塞的，这体现在：本地存储cookie的情况下，无论是导航栏跳转，点击“编辑”按键跳转，外部点击飞行器卡片跳转，都会等待opencode连接成功后才会跳转成功，之前会一致阻塞在当前页面（如”编辑“按键持续显示按下状态，导航栏的“对话入口”选项持续显示按下状态，飞行器卡片持续显示按下状态等）
目前存在build后chunk过大的问题


在上一轮对话中设计了点击“编辑”进入对话入口的功能用例，在理想情况下，用户可以通过与Agent交互修改算法源代码。在本轮对话中，设计点击“配置”展开的各项功能用例，包括配置任务使用的数据集，模型预训练参数，训练集和测试集等文件，以及实例的重启，暂停，删除等功能。
